# Inventario del laboratorio: el único "servidor" gestionado es esta misma máquina.
# ansible_connection=local hace que Ansible ejecute las tareas directamente,
# sin SSH — perfecto para practicar sin tocar ningún servidor real.

[laboratorio]
localhost ansible_connection=local

[laboratorio:vars]
ansible_python_interpreter=/usr/bin/python3
